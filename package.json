{
  "name": "booking-hosting",
  "version": "8.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend run build && node scripts/prepare-vercel-output.js"
  },
  "engines": {
    "node": "20.x"
  }
}
